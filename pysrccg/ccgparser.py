def decode(input, agenda, list, N, grammar):
  agenda.append(input[0])
  candidate_output = []
  while (len(agenda)> 0):
    list = []
  return candidate_output

print(decode([('Tha','Vs')],[],[],2,[]))
